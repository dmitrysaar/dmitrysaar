
# ⚽ Bet Manager

![CI](https://github.com/dmitrysaar/dmitrysaar/actions/workflows/ci.yml/badge.svg)

Это приложение для расчёта прибыли со ставок на футбол. Изначально идея была создать просто базу данных под ставки других прогнозистов на ЧМ 2026 и считать их результаты, отрабатывая при этом SQL запросы, но в дальнейшем уже с базой данных идея переросла в желание попробовать пройти SDLC с подключением тестирования уже на ранних этапах. (QA)

> Моя роль: Выступил в роли QA тестировщика(Серый и черный ящик), составил начальные требования, продумал логику приложения с нуля.
> Важно! Код реализован с помощью ИИ-ассистента по моим требованиям.

В целом - это учебный pet-проект для отработки навыков тестирования, погружения в цикл разработки и тестирования. 

## Что умеет приложение :

- Ведёт небольшой учёт матчей, возможно, в дальнейшем можно будет реализовать функцию определения "любимой" команды, с которой получена бОльшая прибыль.
- Позволяет добавлять ставки на матчи для дальнейшего расчета результата с них (тип ставки, сумма ставки, коэффициент, результат)
- Фиксирует дальнейший результат ставки : Выигрыш, проигрыш, или возврат.
- Рассчитывает чистую прибыль по прогнозисту (беттору) за выбранный период времени.
- Важно! Т.к. приложение задумано, как учебное и не для релиза - в нём нет классического создания нового пользователя. Оно реализуется только через API.

## Логика расчёта прибыли :

Приложение считает именно чистый результат со ставки, а не сумму вместе с ставкой.
Например, беттор поставил 1000 рублей на коэффициент 1.85 и ставка выиграла. Его чистая прибыль со ставки составит 850 рублей. 1000 * (1.85 -1) = 850 рублей.
При проигрыше получаем результат прибыли в -1000.
При возврате получаем результат прибыли в 0 (ничего не выиграли, ничего не проиграли)

## Тест-документация (пополняется и обновляется)

- **Тест-кейсы и чек-листы:** 200+ проверок в Google Sheets *(ссылка будет добавлена)*.
Для больше удобства и возможности "расшарить" решил использовать именно Google Sheets. Всё оформлено в читабельный и понятный вид. Тест-кейсы имеют структуру : 1. Модуль (например Модальное окно : Рассчитать прибыль), ID, заголовок проверки, предусловие, шаги для выполнения проверки, ожидаемый результат и статус. Чек-листы : Модуль, ID, проверка, статус.
API - позитивные и негативные тест кейсы, фронтенд - чеклист. Так же добавлены вкладки с версиями приложения и требованиями.
Использовал такие техники тест-дизайна, как эквивалентное разбиение (группа значений, которая обрабатывается одинаково) и граничные значения. С помощью декомпозиции разбил приложение на модули.
- **Postman-коллекция:** `tests/b_app.postman_collection.json`
- **Newman-отчёты:** `tests/report_*.txt`
- **DDT-данные:** `tests/test_data_for_*.json`
- **Отчёты о дефектах:** *(ссылка будет добавлена)*

## Стек

- **Backend:** Python 3, FastAPI, Pydantic, SQLite
- **Frontend:** HTML, CSS, Vanilla JS
- **Тестирование:** Postman, Newman, ручное тестирование

## Как запустить

### 1. Установить зависимости
```bash
pip install -r requirements.txt
```

### 2. Создать базу данных
```bash
sqlite3 bet_base.db < schema.sql
```
*(если `sqlite3` CLI не установлен — можно выполнить `schema.sql` через DB Browser for SQLite или DBeaver)*

### 3. Запустить сервер
```bash
python main.py
```

Приложение будет доступно по адресу: http://127.0.0.1:8000

### 4. Открыть фронтенд
Открыть файл `index.html` в браузере.


## 📁 Структура проекта

```
bet-manager/
├── main.py                 # Backend (FastAPI)
├── index.html              # Frontend
├── schema.sql              # Схема базы данных
├── requirements.txt        # Зависимости
├── bet_base.db             # Локальная БД (не в git)
└── tests/                  # Тестовые артефакты
    ├── b_app.postman_collection.json
    ├── report_*.txt
    └── test_data_for_*.json
```

## TODO / планы

- [ ] Настроить CI через GitHub Actions
- [ ] Добавить автоматические тесты (pytest)
- [ ] Добавить ссылку на тест-кейсы в Google Sheets
- [ ] Реализовать определение «любимой» команды по прибыли